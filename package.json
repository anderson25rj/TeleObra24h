{
  "name": "teleobra24h",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "dev:v1": "turbo run dev --filter=v1",
    "dev:v2": "turbo run dev --filter=v2",
    "dev:v3": "turbo run dev --filter=v3",
    "build:v1": "turbo run build --filter=v1",
    "build:v2": "turbo run build --filter=v2",
    "build:v3": "turbo run build --filter=v3"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@9.2.0"
}
